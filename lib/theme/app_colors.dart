import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF00B77D);     // Main green color
  static const Color secondary = Color(0xFF038059);   // Darker green for buttons
  static const Color textLight = Color(0xFFFCFDFF);   // Light text color
  static const Color white = Color(0xFFFFFFFF);       // Pure white

  // You can add more color variations here as needed
  static const Color primaryLight = Color(0xFF1AC38D);
  static const Color primaryDark = Color(0xFF00965E);
}